# 2026-09-09 改修前への復帰

今回の開始時点はコード `f1b2aa2`、公開Worker `5493876f-8a3c-45ac-b7ea-5a96c98d1501`。直前の地図改善版に戻す場合は次を実行する。

```sh
npx wrangler rollback 5493876f-8a3c-45ac-b7ea-5a96c98d1501 --name geo-decision-agent --message 'Restore pre-enterprise release 2026-09-09' --yes
```

コード・静的アセットが戻る。D1とR2の新しい記録は削除しない。0010は開示資料の履歴テーブル、0011は現地記録の追加列で、いずれも既存データを削除しない追加変更。旧コードは新しい列・履歴を利用しない。旧画面では新たに登録した動画を正しく再生できない場合があるが、媒体はR2に残る。復帰後はログイン、既存10mメッシュ、既存記録をChromeで確認する。

## コード・独立バックアップ

- 元のコードのタグ: `backup/pre-enterprise-20260909-f1b2aa2`
- 中間版: `backup/enterprise-stage1-2b965ae`
- ブランチ: `codex/mesh-experience-repair`
- 保存先: `/Users/okamurahiroyuki/Documents/Codex/2026-09-09/Geo-Decision-Agent-backup`
- `pre-enterprise.bundle`: 改修前のGit履歴。
- `pre-enterprise-d1.sql`: 改修前のD1全体。認証情報を含むため非公開保管。通常の復帰に流し込まない。
- `deployments-before.json` / `deployments-pre-release.json`: 公開版の照合記録。

```sh
git worktree add --detach ../Geo-Decision-Agent-pre-enterprise backup/pre-enterprise-20260909-f1b2aa2
```

今回、既存のR2媒体は更新・削除していない。R2の独立コピーは作成していない。データ復旧が必要な場合はD1バックアップを別DBに復元して差分を確認する。元の `claude/ai-chat-app-build-s7fut7` ブランチへのpushは既存CIの本番更新を起動するため、復帰手段として使わない。

根拠: [Cloudflare Worker rollback](https://developers.cloudflare.com/workers/versions-and-deployments/rollbacks/)、[D1 migrations](https://developers.cloudflare.com/d1/reference/migrations/)。Wrangler 4.127.1でdry-runを確認。

## 公開記録（2026-09-10）

- 公開日時: 2026-09-10 00:38 JST。
- 改修コード: `cb41d1349a0656c3c9da65c036555f5da43d10b3`。
- 改修版タグ: `backup/enterprise-release-cb41d13`（GitHubにも保存）。
- 公開Worker: `f6c20cff-6792-44e9-9a16-b004ced46d24`、配信割合100%。
- 直前版への復帰先: `5493876f-8a3c-45ac-b7ea-5a96c98d1501`。冒頭のコマンドを使用。
- 追加変更 `0010` / `0011` は本番適用済み。既存記録・媒体の削除なし。
- 公開直前のD1全体: `pre-release-d1-20260910.sql`。上記バックアップフォルダー内、権限600で保管。
- 公開確認記録: `deployments-after.json`。改修後の全Git履歴: `enterprise-release.bundle`。

公開後、ログイン済みChromeで3Dの近景、球体へのズームアウト、解析範囲への復帰、参考写真付き生物カード、既存チャットのMarkdown、新しい開示準備画面の本番データ読み込みを確認した。既に開いていたタブには旧ビルドが残っていたため、ページを読み直して新しいアセットを確認した。
