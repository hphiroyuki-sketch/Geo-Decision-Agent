# 2026-09-08 改善前の版に戻す

元のコード・公開Worker・D1データを分けて保存しています。今回、DBスキーマ変更と写真の変更はありません。

## 公開アプリを戻す

リポジトリで次を実行します。対象は `geo-decision-agent` です。

```sh
npx wrangler rollback fdc15e57-f448-464a-ae3d-160bb5c710ba --name geo-decision-agent --message 'Restore pre-Astra release 2026-09-08' --yes
```

これはコードと静的アセットを戻します。D1の記録やR2の写真は巻き戻りません。利用者が改善後に作成したデータを消さず、表示・処理だけを戻したい場合の手順です。バインディングやシークレットを後から変更した場合は、その互換性も確認してください。完了後、Chromeで再読み込みし、ログイン・地図・既存記録を確認します。

## コードを別フォルダに復元する

```sh
git worktree add --detach ../Geo-Decision-Agent-original backup/pre-astra-20260908-620ca71
```

保存タグ: `backup/pre-astra-20260908-620ca71`、元コミット: `620ca71`。改善ブランチは `codex/mesh-experience-repair`。元の `claude/ai-chat-app-build-s7fut7` ブランチへのpushは既存CIが本番を上書きするため、改善を取り込む前に不用意にpushしないでください。

## 手元の独立バックアップ

場所: `/Users/okamurahiroyuki/Documents/Codex/2026-09-08/Geo-Decision-Agent-backup`

- `pre-astra-620ca71.bundle`: Git履歴。リポジトリ自体を失った場合もclone可能。
- `pre-astra-d1.sql`: 2026-09-08朝のD1全体エクスポート。認証情報を含むため非公開保管。
- `deployments-before.json`: 改善前の公開バージョン記録。

SQLを本番へ直接流し込むと、その後の記録を失う可能性があります。通常のコード復元には使いません。データ復旧が必要な場合は別DBへ復元し、対象と差分を確かめてから切り替えます。R2の独立バックアップは今回作成していません。今回の変更では写真を更新・削除していません。

根拠: [Cloudflare Workers rollback](https://developers.cloudflare.com/workers/versions-and-deployments/rollbacks/)。Wrangler 4.127.1のhelpでもコマンド構文を確認しました。
